//! Persistence layer: database setup/queries and the row types they return.

/// Database connection setup and queries.
pub mod db;
/// Row types stored in and read from the database.
pub mod player;
